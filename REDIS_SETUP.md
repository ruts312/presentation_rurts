# Redis Кеширование (Опционально)

## Зачем нужен Redis?

Redis используется для кеширования:
- 🎤 **TTS аудио** - чтобы не генерировать одно и то же аудио повторно
- 💬 **QA ответы** - для быстрого получения повторяющихся ответов
- ⚡ **Производительность** - значительно ускоряет работу приложения

## Установка Redis (Windows)

### Вариант 1: WSL (Рекомендуется)
```bash
# В WSL Ubuntu
sudo apt update
sudo apt install redis-server
sudo service redis-server start
```

### Вариант 2: Docker
```bash
docker run --name redis-presentation -p 6379:6379 -d redis:latest
```

### Вариант 3: Memurai (Windows Native)
Скачать с: https://www.memurai.com/

## Настройка

Добавьте в файл `backend/.env`:
```env
REDIS_HOST=localhost
REDIS_PORT=6379
```

## Проверка работы

После запуска backend вы увидите:
- ✅ `Redis кеш активирован` - если Redis доступен
- ⚠️ `Redis не доступен, кеширование отключено` - если Redis не установлен

**Приложение работает в обоих случаях!** Redis не обязателен.

## Преимущества с Redis

### Без Redis:
- Каждый слайд генерирует аудио заново (~2-5 секунд)
- Каждый вопрос обрабатывается полностью

### С Redis:
- Повторные слайды загружаются мгновенно
- Повторные вопросы отвечаются за миллисекунды
- Экономия API запросов к OpenAI и HuggingFace

## Очистка кеша

```bash
# Подключиться к Redis
redis-cli

# Очистить весь кеш
FLUSHALL

# Посмотреть все ключи
KEYS *

# Посмотреть статистику
INFO stats
```

## Примечания

- TTL для TTS кеша: 24 часа
- TTL для QA кеша: 1 час
- Кеш автоматически очищается по истечении TTL
