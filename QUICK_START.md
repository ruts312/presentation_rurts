# 🚀 Быстрый запуск проекта

## ✅ Что уже сделано

- ✨ 25 подробных слайдов с эмодзи и иконками
- 🎬 Стартовый экран с кнопкой запуска
- 🎯 Автоматическое переключение слайдов после озвучивания
- 💬 Чат с ИИ внизу экрана (текст + голос)
- 🔊 Голосовые ответы от ИИ
- ⚡ Redis кеширование (опционально)

## 🏃 Как запустить

### Вариант 1: Автоматический запуск (Рекомендуется)
```bash
# Двойной клик на:
start_all.bat
```

Или в PowerShell:
```powershell
.\start_all.bat
```

### Вариант 2: Ручной запуск

#### Backend:
```powershell
cd backend
python main.py
```
Backend запустится на: **http://localhost:8000**

#### Frontend:
```powershell
cd frontend
npm run dev
```
Frontend запустится на: **http://localhost:5174** (или 5173)

## 🌐 Открыть презентацию

После запуска откройте в браузере:
- **Frontend**: http://localhost:5174
- **Backend API**: http://localhost:8000
- **API Docs**: http://localhost:8000/docs

## 🎮 Использование

### 1. Стартовый экран
- Увидите красивый экран с описанием
- Нажмите **"▶️ Запустить презентацию"**

### 2. Автоматическая презентация
- Слайды автоматически озвучиваются
- После озвучивания переключаются автоматически
- Кнопка **⏸️ Пауза** - остановить
- Кнопка **▶️ Воспроизвести** - продолжить

### 3. Навигация
- **◀️ Назад** - предыдущий слайд
- **▶️ Далее** - следующий слайд
- Прогресс-бар и точки показывают позицию

### 4. Чат с ИИ (внизу)
**Текстовый вопрос:**
1. Кликните на панель чата (если свернут)
2. Введите вопрос
3. Нажмите Enter или "📤 Отправить"

**Голосовой вопрос:**
1. Нажмите красную кнопку 🎤
2. Говорите вопрос
3. Нажмите ⏹️ чтобы остановить запись
4. ИИ ответит текстом + голосом

## ⚙️ Настройки API

Отредактируйте `backend/.env`:
```env
# OpenAI для Q&A
OPENAI_API_KEY=your-openai-key

# HuggingFace для TTS
HUGGINGFACE_API_KEY=your-huggingface-key

# Redis (опционально)
REDIS_HOST=localhost
REDIS_PORT=6379
```

**Примечания:**
- Без API ключей будут использоваться mock-данные
- Redis не обязателен, приложение работает и без него

## 🐛 Проблемы?

### Backend не запускается
```powershell
# Установите зависимости
cd backend
pip install -r requirements.txt
python main.py
```

### Frontend не запускается
```powershell
# Установите зависимости
cd frontend
npm install
npm run dev
```

### Ошибка "Ошибка загрузки слайдов"
- Проверьте, что backend запущен на порту 8000
- Откройте http://localhost:8000/api/slides в браузере
- Должен отобразиться JSON с слайдами

### Нет звука
- Проверьте громкость системы
- Откройте консоль браузера (F12) для ошибок
- TTS работает с mock-данными если нет HUGGINGFACE_API_KEY

## 📦 Что входит в проект

```
presentation_ruts/
├── backend/
│   ├── main.py                 # FastAPI сервер
│   ├── requirements.txt        # Зависимости Python
│   ├── .env                    # API ключи (создайте сами)
│   ├── data/
│   │   └── slides.json        # ✨ 25 слайдов с эмодзи
│   ├── routers/
│   │   ├── slides.py          # API слайдов
│   │   ├── tts.py             # 🔊 Text-to-Speech с кешем
│   │   ├── stt.py             # 🎤 Speech-to-Text
│   │   └── qa.py              # 🤖 Q&A с ИИ
│   └── services/
│       ├── cache.py           # ⚡ Redis кеширование
│       ├── huggingface_tts.py # TTS сервис
│       ├── whisper_stt.py     # STT сервис
│       └── openai_qa.py       # OpenAI GPT сервис
├── frontend/
│   ├── src/
│   │   ├── components/
│   │   │   ├── Presentation.tsx  # 🎬 Главный компонент
│   │   │   ├── Slide.tsx         # Отображение слайда
│   │   │   ├── AudioPlayer.tsx   # Аудио плеер
│   │   │   └── VoiceRecorder.tsx # 🎤 Запись голоса
│   │   ├── hooks/
│   │   │   ├── useAudio.ts       # Хук для аудио
│   │   │   └── useVoiceRecorder.ts # Хук для записи
│   │   └── services/
│   │       └── api.ts            # API клиент
│   ├── package.json
│   └── vite.config.ts
├── start_all.bat              # 🚀 Запуск всего
├── start_backend.bat          # Backend отдельно
├── start_frontend.bat         # Frontend отдельно
├── NEW_FEATURES.md            # 🎉 Описание новых функций
├── REDIS_SETUP.md             # Redis инструкции
└── README.md                  # Основная документация
```

## 🎉 Готово!

Теперь у вас есть полнофункциональная интерактивная презентация с:
- ✅ 25 слайдами
- ✅ Стартовым экраном
- ✅ Автоматическим воспроизведением
- ✅ Чатом с ИИ
- ✅ Голосовым управлением
- ✅ Красивыми эмодзи
- ✅ Redis кешированием

Наслаждайтесь! 🚀
