# 🚀 Первый запуск - Пошаговая инструкция

## Шаг 1: Получение API ключей

### OpenAI API Key (ОБЯЗАТЕЛЬНО)

1. Перейдите на https://platform.openai.com/
2. Зарегистрируйтесь или войдите в аккаунт
3. Перейдите в раздел "API Keys": https://platform.openai.com/api-keys
4. Нажмите "Create new secret key"
5. Скопируйте ключ (он начинается с `sk-`)
6. **ВАЖНО**: Сохраните ключ, он больше не будет показан!

### Hugging Face API Key (ОПЦИОНАЛЬНО)

1. Перейдите на https://huggingface.co/
2. Зарегистрируйтесь или войдите
3. Перейдите в Settings → Access Tokens: https://huggingface.co/settings/tokens
4. Нажмите "New token"
5. Дайте название и выберите "Read" права
6. Скопируйте ключ (начинается с `hf_`)

## Шаг 2: Настройка Backend

1. Откройте папку `backend`
2. Создайте файл `.env` (скопируйте из `.env.example`):

```bash
cd backend
copy .env.example .env
```

3. Откройте файл `.env` в текстовом редакторе
4. Вставьте ваши API ключи:

```env
OPENAI_API_KEY=sk-ваш-реальный-ключ-здесь
HUGGINGFACE_API_KEY=hf_ваш-ключ-здесь
HUGGINGFACE_TTS_MODEL=facebook/mms-tts-rus
```

**Примечание**: Без Hugging Face ключа TTS будет работать с mock аудио (тишина).

## Шаг 3: Установка зависимостей Backend

```bash
cd backend

# Создать виртуальное окружение
python -m venv venv

# Активировать (Windows)
venv\Scripts\activate

# Установить зависимости
pip install -r requirements.txt
```

## Шаг 4: Установка зависимостей Frontend

Откройте новый терминал:

```bash
cd frontend
npm install
```

## Шаг 5: Запуск приложения

### Вариант A: Автоматический запуск (рекомендуется)

Просто дважды кликните на файл:
```
start_all.bat
```

Это запустит оба сервера автоматически.

### Вариант B: Ручной запуск

**Терминал 1 - Backend:**
```bash
cd backend
venv\Scripts\activate
python main.py
```

**Терминал 2 - Frontend:**
```bash
cd frontend
npm run dev
```

## Шаг 6: Открытие приложения

Откройте браузер и перейдите на:
```
http://localhost:5173
```

## 🎉 Готово!

Теперь вы можете:
- Просматривать слайды
- Слушать озвучку
- Задавать вопросы голосом
- Получать AI-ответы

## ⚠️ Возможные проблемы

### "OPENAI_API_KEY not found"
- Убедитесь, что файл `.env` находится в папке `backend`
- Проверьте, что ключ начинается с `sk-`
- Перезапустите backend сервер

### "Port 8000 already in use"
- Закройте другие приложения на порту 8000
- Или измените порт в `backend/main.py` (строка: `port=8000`)

### "Port 5173 already in use"
- Закройте другие Vite серверы
- Или измените порт в `frontend/vite.config.ts`

### Микрофон не работает
- Дайте браузеру разрешение на доступ к микрофону
- Используйте Chrome, Firefox или Edge (Safari может иметь проблемы)
- Проверьте настройки микрофона в Windows

### Нет звука в ответах
- Проверьте Hugging Face API ключ
- Или используйте mock режим (звук будет тишиной, но текст будет работать)

## 💰 Стоимость использования

### OpenAI API:
- **Whisper** (STT): ~$0.006 за минуту аудио
- **GPT-4**: ~$0.03 за 1000 токенов (запрос) + $0.06 за 1000 токенов (ответ)

Примерная стоимость одной сессии (10 вопросов): **$0.50 - $1.00**

### Hugging Face:
- Бесплатный tier: 30,000 символов/месяц
- Pro tier: $9/месяц - неограниченно

## 📝 Следующие шаги

1. Изучите документацию API: http://localhost:8000/docs
2. Посмотрите код в `backend/` и `frontend/src/`
3. Добавьте свои слайды в `backend/data/slides.json`
4. Настройте стили в `frontend/src/index.css`

## 🆘 Нужна помощь?

Проверьте:
- [README.md](README.md) - Полная документация
- [ARCHITECTURE.md](ARCHITECTURE.md) - Архитектура проекта
- Логи в терминалах backend и frontend
